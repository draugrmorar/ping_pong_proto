# PING PONG PROTO

gRPC проект для демонстрации базового взаимодействия по протоколу gRPC.
Этот проект создан только для импорта в основной проект, в нем хранится контракт и сгенерированные файлы.
На основании этого проекта можно написать сервис принимающий Ping и отвечающий Pong.

## 🎯 Описание
Проект демонстрирует минимальную настройку gRPC сервера на Go:
- Определение сервиса в .proto файле
- Генерация Go кода из protobuf
- Удобный Makefile для всех операций

## 🔧 Требования
- Go 1.26 или выше
- Protocol Buffers Compiler (protoc)
- Go плагины для protoc:
```bash
  make install
```

## 🚀 Быстрый старт
- Клонируйте репозиторий
```bash
  git clone https://github.com/draugrmorar/ping_pong_proto.git
  cd ping_pong_proto
```

- Внесите свои изменения в контакт в файле [pingpong.proto](./pingpong/pingpong.proto)
- Убедитесь что у вас установлены плагины для работы с protoc:
```bash
  make install
```

- Сгенерируйте новые прото-файлы по новому контракту
```bash
  make regen
```

- Внесенные изменения необходимо запушить на github для дальнейшего использования в других проектах:
```bash
  git add .
  git commit -m "Что нового появилось в контракте"
  git push 
```

- Создаем и запушим новый тег (Нам необходима версия контракта). Сначала проверяем какие теги уже существуют, инкрементируем минорную версию на 1. Навешиваем новый тег и пушим его на github:
```bash
  git tag -l
  git tag v0.x
  git push origin v0.x           
```

- Импортируем сгенерированный контракт в другом проекте:
```bash
   go get github.com/draugrmorar/ping_pong_proto@latest
```


## 📁 Структура проекта
```text
.
├── gen
│   └── go              // тут хранятся сгенерированные файлы 
├── pingpong
│   └── pingpong.proto  // контракт protobuf
├── Makefile            
├── README.md
├── go.mod
└── go.sum
```

## 🛠 Makefile команды
| Команда	     | Описание                                                   |
|--------------|------------------------------------------------------------|
| make install | Установка Go плагинов для protoc                           |
| make gen     | Сгенерировать файлы по контракту                           |
| make regen   | Удалить старые сгенерированные файлы и сгенерировать новые |
| make clean   | Удалить сгенерированные файлы                              |

## 📬 Примеры запросов по текущему контракту:
Запрос:
GRPC HOST:PORT/pingpong.Ping/Ping
```json
{
"ping": "ping"
}
```
Ответ:
```json
{
"pong": "pong"
}
```
